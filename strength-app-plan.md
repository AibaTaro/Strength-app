# 筋トレアプリ：開発計画・仕様・開発エージェント指示

作成日：2026-09-14。更新日：2026-09-14（確定事項を反映）。状態：実装中。

## 0. 確定事項と未確認事項（要約）

### 確定事項
- 利用者は本人1人。ログイン・課金・利用者管理・複数端末同期は今回の開発対象外。
- スマホ向けWeb、端末内保存、書き出し・復元を基本方針とする。将来への備えは、個人データ・種目データ・提案ロジックの分離に留める（同期基盤は作らない）。
- 保有器具（可変式ダンベル「フレックスベル」2個、ベンチ「STEADY ST123」、プッシュバー）は確定し初期登録済み。設定画面から変更可能。
- ダンベルの利用可能重量は片手あたり `[3, 5, 7, 9, 12, 14, 16, 18, 21, 23, 25, 27, 30, 32, 34, 36]` kgの16段階で固定。市販品の一般仕様で上書きしない。
- ダンベル表示・入力は常に「1個あたりの重量」。実績には重量に加え使用個数・左右区分を保存し、負荷量計算で二重計上しない。
- 段階1を最初の動作確認地点とし、段階2〜4まで順次実装する（「段階1だけ実装」ではない）。種目提案・記録・個人設定の3機能がつながる状態を実装の到達点とする。
- 本人による2週間の利用検証は、実装完了後の評価として別に行う。

### 未確認事項（実装は進めるが数値・仕様を断定しない）
- ベンチ「STEADY ST123」の角度調整の具体的な段階・可否。公式情報で確認できるまで、特定角度を要する種目を利用可能と断定しない。基本機能（フラットベンチとしての利用）の実装は進める。
- フォーム教材（動画等）の出典・利用条件。確認できるまで種目に確認済み教材を割り当てず、未確認として表示する。
- 年齢・現在体重・目的・経験・過去の実績の実データ。捏造せず、初回設定・初回調整の入力画面から本人が取得する。

## 1. 目的と仮定

「今日は何をどれだけやるか決まり、運動中に迷わず記録でき、次回の提案が実績から改善する」を中心体験にする。

確定要件は種目提案、記録、個人プロファイリングの3機能。

- 本人専用として開発する。一般公開は今回の開発対象外（将来やる場合は別工程・別見積もり）。
- スマホ向けWeb。実機（Android/iPhoneのブラウザ）で確認する。
- 自宅のダンベル（フレックスベル）、ベンチ（STEADY ST123）、プッシュバー、自重を対象に種目を収録する。段階1は5種目、段階4で15〜20種目に拡張。
- 初期対象は一般的な成人向け運動。リハビリや疾患別の運動処方は扱わない。
- 端末内保存＋書き出し・復元。複数端末同期は対象外。
- 作業時間・予算・希望納期は未確認。工程の工数は仮見積もり。

## 2. 器具（確定事項）

初期登録する3種類の器具。設定画面から重量一覧・個数等を変更可能にする（変更してもこの初期値を上書き前提にしない＝あくまで初期シードデータ）。

### 可変式ダンベル「フレックスベル」
- 個数：2個
- 片手あたりの利用可能重量（16段階、kg）：`3, 5, 7, 9, 12, 14, 16, 18, 21, 23, 25, 27, 30, 32, 34, 36`
- 名称はユーザー申告。市販品の一般仕様からこの重量一覧を推測・上書きしない。
- 表示・入力は常に「1個あたりの重量」。2個使用時は「片手12kg×2個」、1個を両手で持つ種目は「12kg×1個」のように表示する。

### ベンチ「STEADY ST123」
- 角度調整の仕様は未確認。公式情報で確認できるまで、特定の角度を必要とする種目（インクライン・デクラインなど）を利用可能と断定しない。
- フラットベンチとしての基本機能（種目の土台）は実装を進める。

### プッシュバー
- 腕立て伏せ（自重種目）の補助器具として扱う。
- 自重種目の実績とダンベル種目の重量実績は区別して保存・集計する。「体重と同じ重量」としては集計しない。

## 3. 初期版の機能

| 分野 | 初期版で作る | 対象外・後続 |
|---|---|---|
| 種目提案 | 部位・器具・目的・所要時間による絞り込み、名称、説明、確認済み/未確認の教材表示、回数・セット・重量の候補、提案理由、種目交換 | 3D教材、ジム種目、長期計画、自然言語相談 |
| 記録 | セット単位の回数・重量・使用個数・左右・余力・痛み、前回値の表示、休憩タイマー、開始・一時停止・終了、修正・削除 | 自動回数計測、外部機器連携 |
| 集計 | 前日・直近7日・直近1か月、実施日数・時間・セット数・種目別推移・体重推移 | 長期予測、共有レポート |
| プロファイル | 年齢・身長・体重・目標体重・目的・経験・器具・頻度・時間・避けたい動作 | 健康データ連携、より詳細な分析 |
| 公開・共有 | （対象外） | 認証、課金、利用者管理、複数端末同期、公開 |

体重目標は任意とし、「維持」も選べる。筋肥大・減量・筋力向上・健康維持から主目的を1つ選び、副目的を任意入力する。体重だけで成果を判定せず、筋肥大なら回数や重量、健康維持なら実施頻度も表示する。減量の達成日や消費カロリーを筋トレ記録だけから断定しない。

フォーム指導は初期版では「教材の提示」であり、「本人の動作が正しいという判定」ではない。撮影映像の自動評価は別機能とする。

## 4. 画面と利用の流れ

1. 初回設定：目的→身体情報→経験→器具と重量一覧（確定済み初期値を表示、変更可）→頻度と時間。
2. 今日の運動：希望部位、使える時間、疲労、痛みを入力。候補を確認・交換し開始。
3. 種目詳細：名称、主対象・補助部位、動画（確認済みのみ、なければ未確認表示）、フォームの要点3つ、よくある誤り、器具、代替種目。
4. 運動中：前回値と今回の提案値を表示。重量（1個あたり）・個数・左右・回数を変更して完了。休憩開始。あと何回できそうだったかを簡単に入力。
5. 振り返り：実績、期間集計、体重推移、次回候補と理由を確認。

設定は独立画面に置く。運動中は大きな入力部品、前回値の引継ぎ、片手操作を優先。文章を読む量を減らす。目標は、値の変更不要なら1操作で1セットを確定できること。

## 5. 提案の設計

### 原則

年齢・体重だけから適正重量は決めない。初回は種目別に軽い負荷で試し、実行回数・本人の余力・フォームの自己申告から基準を作る。履歴がないときは「初回調整が必要」と表示し、架空の重量を断定しない。

米国スポーツ医学会の2026年の解説は、目的などに応じた個別化と継続を重視している。ただし、以下のアプリの条件分岐や数値は同学会が検証したアルゴリズムではなく、実装・専門家確認用の仮ルールである。
参照：https://acsm.org/resistance-training-guidelines-update-2026/

### 処理順序

1. 対象部位と利用器具で候補を絞る。
2. 痛みや避けたい動作に該当する候補を除外する。除外後に候補がなければ、その旨を表示し無理に生成しない。
3. 確認済み教材の有無にかかわらず候補にはするが、教材は確認状態を明示する。直近の実施、当日の疲労、目的、経験から順位を付ける。
4. 選択部位を優先しつつ、偏りは理由付きで知らせる。本人の指定を黙って変更しない。
5. 種目別の準備・セット実施・休憩・切替時間を足し、使える時間に収める。
6. 回数・セット数は目的別・種目別の設定表から取得。重量は同一種目の履歴と利用可能重量（16段階）から決める。
7. 提案理由、参照した履歴、ルールの版、本人による変更を保存する。

### 重量調整のルール（ダンベル種目）

- 重量候補は登録済み16段階 `[3,5,7,9,12,14,16,18,21,23,25,27,30,32,34,36]` から選ぶ。この範囲外の重量（3kg未満・36kg超）や16段階にない中間値（11kgなど）を捏造しない。
- 重量選択の＋／−操作は、金額入力のような任意刻みではなく、必ず16段階の「隣の登録重量」へ移動する（9kgの次は12kg、11kgは生成しない）。
- 次回の増量提案は単純な「前回重量＋2kg」にしない。16段階の実際の刻み幅を使う。特に9→12kg（+3）、18→21kg（+3）、27→30kg（+3）のように、段によって増加幅が2kgではなく3kgになる区間があることを考慮する。
- 増量条件（仮ルール）：直近2回の実施で全対象セットが目的別の回数上限に達し、余力が2回以上、痛み・フォームの問題がない場合に「次の段階」を候補にする。
- 次の段階への刻みが種目別の増加上限設定を超える場合（刻みが大きすぎる場合）は自動増量せず据え置く。据え置き時は、設定した回数範囲内での回数調整候補（例：上限回数をさらに数回増やす提案）を示す。単純な自動増量はしない。
- 下限未達、余力なし、疲労が強い場合は据え置きや減量の候補を出す。休止期間が長い場合は再調整する。
- 最小重量（3kg）でも実施が難しい場合、無理に3kg未満を作らず、対応する代替種目（自重・可動域を減らすバリエーション等）を案内する。
- 最大重量（36kg）に達した場合、それ以上の増量や際限のない回数増加を強制しない。回数・セット数の据え置き、または種目交換を候補にする。
- 痛みがある場合は増量提案を停止し、その種目の中止導線を出す。医学的診断や治療法を生成しない。
- 最終的な重量・個数・左右は本人が確認して変更可能。提案を勝手に実績として保存しない。
- 回数範囲・セット数・増加上限・休憩秒数・再調整条件・重量段階はコードに散在させず、設定表で管理する。公開する場合は運動指導の専門家が確認する（今回は非公開のため必須ではないが、検証可能な形にしておく）。

初期版の提案は条件分岐で実装する。生成AIを後から入れる場合も、検証済み結果の説明や相談を担当させ、重量・除外条件・集計の最終決定は検証可能な処理に残す。

## 6. 記録と集計の定義

- 重量はkg。ダンベルは常に「1個あたりの重量」で入力・表示する。両手合計などの表示はしない。
- セット実績には、重量（1個あたり）に加えて「使用個数（1個／2個）」と「左右区分（左／右／両側同時／区別なし）」を保存する。片側ずつ実施する種目は左右それぞれの回数を記録できる。
- 自重は追加重量と区別し、体重をそのまま持ち上げ重量と見なさない。プッシュバーを使う腕立て伏せ等は自重種目として扱い、「体重と同じ重量」としては集計しない。
- 総負荷量（参考値）は「1個あたりの重量 × 使用個数 × 回数」を左右ごとに計算し合算する。使用個数・左右を二重計上しない。
  - 例（検証用データ、運動の推奨値ではない）：2個使用・片手12kg・10回 → 12×2×10 = 240kg。1個使用・左右各10回・12kg → 12×1×10（左）+12×1×10（右）= 240kg。1個を両手で持ち12kg・10回 → 12×1×10 = 120kg。
- 集計は完了したセットを対象とし、未完了・削除済みを除外。準備セットと本セットは別集計にする。
- 総負荷量は入力重量を種目定義に従い正規化して回数を掛ける。同一種目内の参考値とし、自重・ダンベルをまとめて能力点にしない。
- 部位別セット数は初期版では主対象部位だけに加算。補助部位は別表示して二重計上を避ける。
- 初期タイムゾーンはAsia/Tokyo（日本時間）。前日は前日0時〜当日0時。直近7日は今日を含む7暦日。直近1か月は1か月前の同日の翌日〜今日で、月末は存在する最終日に補正。画面に必ず期間を表示する。
- 比較対象は直前の同日数。比較元が0なら増減率は出さず実数差だけ表示。記録なしは「記録なし」とし体重0を補完しない。
- セットは実施時刻で集計。実施日数はセットがある日を数える。運動時間は休憩を含み一時停止を除外し、日跨ぎは日ごとに配賦する。
- セッション経過時間と休憩タイマーを分ける。経過時間は時刻差から復元し、画面の更新回数を数えて計測しない。
- 画面を閉じた後の通知は保証しない。復帰後に残り時間を再計算。バックグラウンド通知の要否でアプリ化を判断する（今回はWebのみ）。
- 実績の修正・削除後は集計と次回提案を再計算。過去に提示した提案そのものは履歴として保持する。
- 書き出し・復元では器具（種類・重量一覧・個数）、単位、使用個数、左右区分を含め、全データが往復して一致することを保証する。

## 7. 最低限のデータ設計

| データ | 主な項目 |
|---|---|
| 個人設定 | 識別子、年齢、身長、目的、経験、頻度、所要時間、避けたい動作、タイムゾーン |
| 体重履歴 | 日付、体重、任意の体脂肪率、記録時刻 |
| 目標履歴 | 目標体重または維持、主目的、副目的、設定日 |
| 器具 | 種類（ダンベル／ベンチ／プッシュバー等）、名称、個数、利用可能重量の配列（ダンベルのみ）、片手基準、ベンチ角度調整可否（未確認） |
| 種目 | 識別子、名称、主部位、補助部位、必要器具、片側区分、重量基準（個あたり／個数）、フォーム、代替種目 |
| 教材 | 種目識別子、URL、出典、権利・利用条件、確認日、確認者、審査状態（確認済み／未確認） |
| 提案 | 個人設定の写し、ルール版、参照履歴、種目、回数、重量、セット、理由、生成時刻 |
| 運動セッション | 開始、終了、一時停止区間、状態、当日の疲労 |
| セット実績 | 固有識別子、セッション、種目、左右区分、使用個数、順序、重量（個あたり）、回数（左右別）、余力、痛み、準備セット区分、完了時刻、更新版 |

固有識別子により二重タップや再送での重複保存を防ぐ。過去の身体情報を新しい情報で上書きしない。教材の未審査状態を「確認済み」と表示しない。

## 8. 技術方針

- 画面：React + TypeScript + Vite。スマホ向けWebとして動作。
- 保存：端末内（localStorage、将来IndexedDBへ移行可能な形）。書き出し（JSONダウンロード）・復元（JSONアップロード）を段階1から実装。
- 認証・サーバー保存・複数端末同期は実装しない（今回の対象外）。
- 提案・集計は画面から独立した関数群（ロジック層）とし、後から画面や保存先を交換できるようにする。個人データ・種目データ・提案ロジックをモジュールとして分離する（将来の同期・複数利用者対応に備えるが、今回は同期基盤自体は作らない）。
- 依存ライブラリの版は実装時に固定する（package.jsonでロック）。
- 外部動画が読めなくても記録を妨げない。説明文と外部リンクを表示する。未確認教材は「未確認」と明示する。
- 実際の身体情報をサンプルデータや公開リポジトリに入れない。生成AIへの身体データ送信は行わない。

## 9. ロードマップ

工数は1人の開発者が開発エージェントを使い、実装・確認・修正する総作業時間の仮見積もり。待ち時間・専門家の教材審査・動画制作は含まない。確約日程ではない。

| 段階 | 作業 | 完了条件 |
|---|---|---|
| 1 | 初期登録済み器具（フレックスベル2個・ベンチ・プッシュバー）、5種目、手動選択、セット記録（重量・個数・左右）、端末保存、書き出し・復元 | 実際に1回筋トレし再読込後も記録が残る。最初の動作確認地点。 |
| 2 | セッション時間、休憩、修正、期間集計 | 記録・時刻・集計の境界条件が通る |
| 3 | プロファイル、初回調整、条件による提案、16段階重量の増量・据え置きロジック | 種目提案・記録・個人設定の3機能がつながる。器具外の重量や除外種目を出さない |
| 4 | 15〜20種目への拡張、教材の確認状態管理 | 教材未確認種目も操作でき、確認済み/未確認が区別表示される |

段階1〜4を今回の実装対象とする。段階1を最初の動作確認地点とし、段階2〜4まで順次実装する（「段階1だけ実装して終わり」ではなく、記録単体で完成とせず種目提案・記録・個人設定の3機能がつながる状態を到達点にする）。各段階で必要な検証を行い、通常の実装判断は自律的に進める。

本人による2週間の利用検証（4〜6回使用）は、段階1〜4の実装完了後に別の評価工程として行う。

最初から作らないもの（今回の対象外）：認証、課金、交流機能、ランキング、食事管理、複数端末同期、端末連携、カメラによるフォーム判定、独自3D生成、一般公開。

### 段階5（公開する場合のみ・今回の実装対象外）

将来、一般公開を検討する場合に必要になる作業の記録として残す。今回の実装指示からは外す。

認証、同期、重複・競合処理、データ削除、復旧手順、利用目的の説明、教材の公開利用条件、専門家によるルール確認、利用者間のデータ隔離の検証。

## 10. 開発エージェントへの共通指示

あなたは筋トレ支援Webサービスの開発担当です。この文書を仕様として使用してください。目的は、種目提案→実行→記録→次回改善の一連の体験を実現することです。利用者は本人1人で、一般公開は対象外です。

以下を守ってください。
- 日本語で表示し、専門用語は意味が分かる表現にする。
- 段階1を最初の動作確認地点とし、段階2〜4まで順次実装する。記録機能だけで完成とせず、種目提案・記録・個人設定の3機能がつながる状態を到達点にする。見た目だけで保存できない画面を完成扱いしない。
- 年齢・体重から重量を断定しない。履歴なしの場合は初回調整と明示する。
- 提案は承認済み種目と利用可能重量（16段階）に限定する。数値や教材URLを生成AIで捏造しない。
- 提案と実績を別に保存し、セット確定時に保存する。
- ダンベルの入力単位は「1個あたりの重量」、使用個数、左右の扱いを曖昧にしない。負荷量計算で二重計上しない。
- 痛みがある種目の増量を止める。未対応の身体条件に対して個別運動処方を生成しない。
- 教材は出典・利用条件・確認状態を保持する。未確認の教材を確認済みと表示しない。ベンチの角度仕様が未確認のうちは角度依存種目を利用可能と断定しない。
- 再読込、二重タップ、保存失敗、空履歴、月末、日跨ぎ、重量刻み（16段階の非均等な刻み）を扱う。
- 提案・集計ロジックを画面から分離する。依存関係は公式文書を確認し、使用版を固定する。
- 身体情報や秘密情報をログ・外部AIへ不用意に送信しない。
- 認証・サーバー保存・複数端末同期は実装しない。

各段階の成果として、動く実装、起動手順、変更内容、検証結果、未実装項目、次段階の課題を提出してください。テストしたことと未確認のことを区別してください。

## 11. 段階別の追加指示

### 指示1：記録を最初に完成させる

初期登録済みの3器具（フレックスベル2個・ベンチ・プッシュバー）を前提に、5種目の一覧、手動選択、重量（1個あたり）・使用個数・左右・回数の入力、セット完了、履歴、編集・削除、端末保存、書き出し・復元を作ってください。提案機能はまだ不要です。前回実績があれば初期入力値に使い、今回分は確定前に実績化しないでください。スマホ幅で操作確認し、再読込と復元で記録が一致することを検証してください。

### 指示2：時間管理と集計

セッションの開始・一時停止・再開・終了、休憩タイマー、前日・7日・1か月の集計を追加してください。本書の時間・重量・期間定義を使ってください。画面復帰、日跨ぎ、月末、比較元0、修正・削除での再集計をテストしてください。

### 指示3：個人設定と提案

個人設定、器具登録（初期値は確定済み3器具）、体重履歴、目標設定、部位指定、初回調整、実績に応じた次回提案（16段階の増量・据え置きルール）を追加してください。提案ルールは版管理してください。重量刻みや除外で候補がない場合の表示を作ってください。数値設定表は専門家確認待ちとして識別してください。

### 指示4：教材と実用検証

対象種目を15〜20に拡張してください。動画等の教材は出典・利用条件が確認できたものだけを「確認済み」として割り当て、未確認のものは「未確認」と明示してください（教材が確認できない場合は無理に確認済み扱いにせず、他の実装を進める）。外部動画が再生不能でも操作できるようにしてください。

## 12. 受入基準と評価

- 1セットを確定して再読込しても保持される。二重タップで増殖しない。
- 書き出し・復元で器具、単位、個数、左右、時刻、実績が一致する。
- 入力ミスの修正が集計へ反映される。
- 画面を閉じて戻っても経過時間が正しい。
- 月末、日跨ぎ、空データで誤集計しない。
- 保有していない重量・器具の種目を提案しない。
- 初回と履歴十分な場合で提案が適切に分かれる。
- 痛み・余力未入力・重量上限到達時に増量を強制しない。
- 動画が読めなくても記録できる。
- 初期設定に3種類の器具とダンベル2個が登録されている。
- 重量選択肢が指定の16段階と完全に一致する。9kgの次の選択肢は12kgで、11kgを生成しない。
- 重量刻みが大きい場合、増量条件を満たさなければ自動増量しない。
- 負荷量計算の検証（推奨値ではなく計算検証用）：2個使用・片手12kg・10回で240kg。1個使用・左右各10回・12kgで合計240kg。1個を両手で持ち12kg・10回で120kg。

実用性の仮目標：変更不要なら1操作で記録、入力変更込みでも中央値5秒以内、2週間で4〜6回使用、記録消失0件。提案の交換理由や重量の修正理由を集める。目標未達なら種目数やAI機能を増やす前に操作性と提案条件を直す。

## 13. 後から3D教材へ広げる場合（参考・対象外）

3Dモデルの動作を専門家が確認→角度別に書き出し→動画またはGIF形式で配信、という独立工程にする。制作・利用許諾・動作確認の工数を別見積もりにする。生成した人物映像や3D動作を、見た目だけで正しいフォームと認定しない。初期版の動画表示部分を差し替え可能にしておけば、記録・提案の仕組みは再利用できる。
