# 検証結果・起動手順・利用検証手順

対象コミット: 実装チェックポイント(段階1〜3の中核機能 + 段階4の種目データ)。
仕様は [`strength-app-plan.md`](./strength-app-plan.md)、実装ルールは [`AGENT.md`](./AGENT.md) を参照。

## 1. 起動手順

```bash
cd app
npm install   # 初回のみ
npm run dev -- --host
```

- ターミナルに `Local: http://localhost:5173/` と `Network: http://<PCのIP>:5173/` が表示される。
- PCのブラウザでは `http://localhost:5173/` を開く。

## 2. スマホでの確認手順

1. スマホとPCを同じWi-Fiに接続する。
2. スマホのブラウザで、`npm run dev -- --host` 実行時に表示された `Network:` のURL(例: `http://192.168.0.xxx:5173/`)を開く。
3. 画面幅375px相当(一般的なスマホ)を想定してレイアウトを作成済み。「今日」「運動中」「振り返り」「設定」の4タブを下部で切り替える。
4. データは端末(ブラウザ)内のlocalStorageに保存される。他の端末やブラウザとは共有されない。
5. 「設定」タブ下部の「データを書き出す」でJSONファイルをダウンロードできる。機種変更やブラウザデータ消去に備え、記録後は定期的に書き出しておくことを推奨する。

## 3. 自動検証結果(2026-09-15時点)

`app/` ディレクトリで実行し、すべて成功を確認済み。

```bash
npm run build   # tsc -b && vite build → 成功
npm run lint    # ESLint → エラー0、警告1件(後述)
npm run test    # vitest → 25件成功
```

### 自動テストで検証した受入基準

- 初期設定に3種類の器具(ダンベル・ベンチ・プッシュバー)とダンベル2個が登録される。
- 重量選択肢が16段階と完全一致し、9kgの次は12kg(11kgを生成しない)。
- 18→21kg、27→30kgのような+3kgの刻みでも正しく次の段階に増量する。
- 種目別の増加上限(例: アイソレーション種目は2kg)を超える刻みでは自動増量せず据え置く。
- 最大重量(36kg)到達後は増量を強制しない。
- 最小重量(3kg)でも回数が届かない場合は代替種目を案内する。
- 直近の実施に痛みの申告があれば増量せず据え置く。
- 2個使用・片手12kg・10回で外部負荷量240kg。1個使用・左右各10回・12kgで合計240kg。1個を両手で持ち12kg・10回で120kg(二重計上なし)。
- 自重種目(プッシュバー使用)は体重と同じ重量として集計しない(負荷量null)。
- 前日/直近7日/直近1か月の期間計算(JST、月末補正、比較期間)。
- 書き出し(JSON化)→取り込みで器具・単位・使用個数・左右が一致する。
- 1セットを保存後、`loadAppData`の再実行(再読込相当)でも保持される。

### 手動でブラウザ確認したこと(スマホ幅375pxでの実機相当確認)

- 初回に部位・時間・疲労を入力し「候補を作る」→ 履歴なしのため全種目が「初回調整」表示、かつベンチ角度未確認によりダンベルインクラインベンチプレスが自動的に候補から除外されることを確認。
- 「この内容で開始」でセッションが開始し、運動中タブに自動遷移。ブラウザを再読込しても「運動中 ●」表示が残り、アクティブセッションが保持されることを確認。
- セット確定後、休憩タイマー(健康維持目的で90秒)が開始することを確認。
- セッション終了後、「振り返り」タブで実施日数・セット数・参考負荷量(3kg×2個×10回=60kg)が正しく表示されることを確認。
- 「設定」タブで目的の変更、器具(重量一覧・ベンチ角度確認・プッシュバー有無)の変更、データ書き出しUIの表示を確認。

### 未確認・今回テストしていないこと

- 実際のスマホ実機(iOS/Android)のブラウザでの動作。今回はブラウザのモバイル幅エミュレーションでの確認まで。
- 長時間(数時間・日をまたぐ)セッションの経過時間配賦の実機確認。ロジックは自動テストで検証済みだが、実際のブラウザのスリープ・バックグラウンド動作での挙動は未確認。
- JSON書き出しファイルを実際にダウンロード→別ブラウザ/別端末で取り込む操作そのもの(ロジックの往復はテスト済み)。
- 二重タップ防止は、確定ボタン押下処理をrefで同期的にガードする実装にしたが、実機での連打操作そのものは未確認。
- 年齢・体重・目的などの個人設定、種目の教材(動画)は未入力・未確認のまま。本人の実データ投入は次の利用検証で行う。
- ESLintの警告1件(`AppContext.tsx`がコンポーネントとフックを同一ファイルからエクスポートしているためのFast Refresh警告)。動作に影響しないため未対応。

## 4. 本人による2週間の利用検証の手順

実装完了後の評価として、以下を行う。

1. **初期設定**: 「設定」タブで年齢・身長・主目的・経験・週の頻度・1回の所要時間を入力する。ダンベルの重量一覧・ベンチ・プッシュバーは初期値のままでよいが、実際と異なる場合はここで修正する。
2. **記録前のバックアップ**: 利用開始前に一度「データを書き出す」でJSONを保存しておく(初期状態の控え)。
3. **通常利用(4〜6回)**: 「今日」タブで部位・時間・疲労を入力して候補を作り、「この内容で開始」→ 運動中タブでセットを記録する。提案に納得できない場合は「種目交換」で変更してよい。
4. **記録後の確認**: 毎回の利用後に「振り返り」タブで前日・直近7日の集計を確認する。
5. **週1回程度のバックアップ**: 「データを書き出す」でJSONを保存する(端末故障・ブラウザデータ消去への備え)。
6. **記録すること**:
   - 記録に手間取った箇所(入力項目・操作手順で迷った点)。
   - 提案に納得できなかった箇所(重量・回数・種目の妥当性)。
   - 増量・据え置きの判断が実感と合っていたか。
   - 痛み申告後の据え置き・中止導線が適切だったか。
7. **2週間後**: 上記の記録をもとに、操作性(値の変更不要なら1操作、入力変更込みで中央値5秒以内が目標)と提案条件の妥当性を見直す。目標未達なら、種目数や機能追加より先に操作性と提案条件を調整する。

この手順による検証結果は、次の実装判断(提案ロジックの調整・画面の改善)に反映する。

## 5. UI/UX刷新とE2Eテスト(2026-09-19)

- UI: ライト/ダーク自動対応のデザインシステム、大きなステッパー(重量・回数)、セグメント選択、進捗リング、休憩バー(+15秒/スキップ)、ボトムシート(種目交換・追加・終了確認)、削除の「元に戻す」、空状態の案内、セーフエリア対応。
- UX: 次の種目の自動展開、片側種目の左右自動切替、目的未設定の案内、運動中の再開バナー、種目を選んで始める(候補なし)、復元前の確認シート。
- E2E(Playwright, Pixel 7): `cd app && npm run test:e2e`(初回のみ `npx playwright install chromium`)。50件、3回連続実行で全件成功。
  候補作成・16段階の増量/据え置き/上限・負荷量240/240/120kg・二重タップ・休憩/一時停止・期間集計・痛み除外・書き出し復元・HTTP環境の回帰・横スクロール/タップサイズ/ダークモードなどを検証。
- E2Eで見つけて修正した不具合: 選択部位が「補助」で一致する種目が、主対象の種目より先に提案されていた。
- 未確認: スマホ実機(iOS/Android)での操作感。
